# 城设 CityBible 配置模板 —— 复制为 .env 后填写
# 注意：地理真实性判定引擎不依赖任何外部 API，不填 key 也能完整运行。
# 以下 key 仅用于可选的「语义补充轨」和素材再生成。
MINIMAX_API_KEY=
ARK_API_KEY=
BOCHA_API_KEY=
CITYBIBLE_API_TOKEN=change-me-before-exposing-mcp
